import os
from dotenv import load_dotenv
from pprint import pprint
import json
import requests

import pandas as pd
import numpy as np

load_dotenv() #look in the ".env" file for env vars


def googleAPI(YELP_API_KEY_parameter, search_term, location_parameter):
    
    


